/** RAG system prompt / instructions for SpeedRay */

export const RAG_SYSTEM_PROMPT = `Use the following context from Kaggle and NHI chest X-ray datasets to inform the diagnostic report. Cite only when relevant.`;

export const RAG_QUERY_PREFIX = 'chest x-ray findings pneumonia opacity consolidation';
